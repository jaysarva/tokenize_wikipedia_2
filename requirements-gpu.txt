# GPU requirements for embedding generation
# Base requirements
ray[default]==2.9.3
tiktoken
requests

# GPU/ML requirements - pinned for compatibility with PyTorch 2.4.0
sentence-transformers==3.0.1
transformers>=4.41.0,<5.0.0
numpy>=1.24.0

# torch is included in the pytorch base image

